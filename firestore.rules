rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 🔐 Funções auxiliares globais
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return request.auth.uid == userId;
    }

    function isExistingOwner(userId) {
      return isOwner(userId) && resource != null;
    }

    // ✅ Checa se o usuário logado é admin via campo `isAdmin` no doc dele
    function isAdmin() {
      return request.auth.uid != null &&
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isAdmin == true;
    }

    /**
     * Users - perfis de usuário
     */
    match /users/{userId} {
      allow get: if isSignedIn() && (isOwner(userId) || isAdmin());
      allow list: if isAdmin(); // só admin pode listar todos os users
      allow create: if isSignedIn() && request.auth.uid == userId;

      // usuário só pode atualizar o próprio doc se não mexer no campo isAdmin
      allow update: if (isSignedIn() && isOwner(userId) 
                          && request.resource.data.isAdmin == resource.data.isAdmin)
                    || isAdmin();

      allow delete: if (isSignedIn() && isOwner(userId)) || isAdmin();
    }

    /**
     * Clients - subcoleção de cada user
     */
    match /users/{userId}/clients/{clientId} {
      allow get, list, create, update, delete: if isSignedIn() && (isOwner(userId) || isAdmin());
    }

    /**
     * Services - leitura pública, escrita só admin
     */
    match /services/{serviceId} {
      allow get, list: if true;
      allow create, update, delete: if isAdmin();
    }

    /**
     * Promotions - leitura pública, escrita só admin
     */
    match /promotions/{promotionId} {
      allow get, list: if true;
      allow create, update, delete: if isAdmin();
    }

    /**
     * Appointments - subcoleção de cada user
     */
    match /users/{userId}/appointments/{appointmentId} {
  // donos só conseguem ver os próprios
  allow get, list, create: if isSignedIn() && isOwner(userId);
  allow update, delete: if isExistingOwner(userId);

  // admins conseguem tudo em qualquer appointment
  allow get, list, create, update, delete: if isAdmin();
}
  }
}
